
.bar {
  height: 4px;
  position: relative;
}

.background {
  width: 100%;
  height: 100%;
  background-color: var(--gray);
  border-radius: 4px;
}

.progress {
  position: absolute;
  top: 0;
  left: 0;
  height: 100%;
  background-color: var(--black);
  border-radius: 4px;
}
