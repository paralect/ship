export {
  type ListParams,
  type ListResult,
  type SortOrder,
  type SortParams,
  type AccountGetResponse as User,
} from 'shared';
export type { ApiError } from 'shared';

export type QueryParam = string | string[] | undefined;
