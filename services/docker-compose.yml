version: '3.6'
services:
  mongo:
    container_name: ship-dev-mongo
    image: mongo:4.4
    entrypoint:
      - bash
      - -c
      - |
        cp /config/mongo-keyfile /config/keyfile
        chmod 400 /config/keyfile
        chown mongodb -R /config/keyfile
        exec docker-entrypoint.sh $$@
    command: mongod --replSet rs --bind_ip_all --keyFile config/keyfile --quiet --logpath /dev/null
    environment:
      - MONGO_INITDB_ROOT_USERNAME=root
      - MONGO_INITDB_ROOT_PASSWORD=root
    networks:
      - ship-dev
    ports:
      - 27017:27017
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ./api-koa/src/config/mongo-keyfile:/config/mongo-keyfile
      - type: volume
        source: mongodb
        target: /data/db
      - type: volume
        source: mongodb-cfg
        target: /data/configdb
  mongo-replicator:
    container_name: ship-dev-mongo-replicator
    build: ./api-koa/mongo-replicator
    environment:
      - HOST=mongo
      - PORT=27017
      - USERNAME=root
      - PASSWORD=root
      - REPLICA_SET_NAME=rs
    networks:
      - ship-dev
    depends_on:
      - mongo
  redis:
    container_name: ship-dev-redis
    image: redis:5.0.5
    command: redis-server --appendonly yes
    hostname: redis
    networks:
      - ship-dev
    ports:
      - 6379:6379
  api-koa:
    container_name: ship-dev-api-koa
    build:
      context: ./api-koa
      dockerfile: Dockerfile
      target: development
      args:
        NODE_ENV: development
        APP_ENV: development
    networks:
      - ship-dev
    volumes:
      - ./api-koa/src:/app/src
    ports:
      - 3001:3001
    depends_on:
      - redis
      - mongo-replicator
  migrator:
    container_name: ship-dev-migrator
    build:
      context: ./api-koa
      dockerfile: Dockerfile.migrator
      target: development
      args:
        NODE_ENV: development
        APP_ENV: development
    networks:
      - ship-dev
    volumes:
      - ./api-koa/src:/app/src
    depends_on:
      - mongo-replicator
  scheduler:
    container_name: ship-dev-scheduler
    build:
      context: ./api-koa
      dockerfile: Dockerfile.scheduler
      target: development
      args:
        NODE_ENV: development
        APP_ENV: development
    networks:
      - ship-dev
    volumes:
      - ./api-koa/src:/app/src
    depends_on:
      - mongo-replicator
  web:
    container_name: ship-dev-web
    build:
      context: ./web
      dockerfile: Dockerfile
      target: development
      args:
        NODE_ENV: development
        APP_ENV: development
    volumes:
      - ./web/src:/app/src
      - ./web/public:/app/public
    networks:
      - ship-dev
    ports:
      - 3002:3002

networks:
  ship-dev:
    name: ship-dev-network

volumes:
  mongodb:
  mongodb-cfg:
