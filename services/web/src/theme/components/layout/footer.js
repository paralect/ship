export const Footer = (theme) => ({
  root: {
    marginTop: 'auto',
    padding: '12px 0',
    textAlign: 'center',
    flex: '0 1 auto',
    backgroundColor: theme.colors.brand[0],
    border: 'none',
    fontSize: '12px',
  },
});
