export const AppShell = (theme) => ({
  root: {
    display: 'flex',
    flexDirection: 'column',
    minHeight: '100vh',
    backgroundColor: theme.colors.brand[0],
  },
  main: {
    padding: '32px',
  },
});
