export const Header = (theme) => ({
  root: {
    minHeight: '72px',
    padding: '0 32px',
    backgroundColor: theme.colors.brand[9],
    display: 'flex',
    alignItems: 'center',
    flex: '0 1 auto',
  },
});
