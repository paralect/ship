import { createStyles } from '@mantine/core';

export const useStyles = createStyles(({ colors }) => ({
  divider: {
    margin: '32px 0',
    padding: '0 12px',
    fontSize: '16px',
    color: colors.gray[6],
  },
}));
