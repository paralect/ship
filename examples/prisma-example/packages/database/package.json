{
  "name": "database",
  "version": "1.0.0",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "private": true,
  "scripts": {
    "prisma:generate": "prisma generate --schema=./prisma/schema.prisma",
    "build": "tsc",
    "prepack": "npm run build"
  },
  "dependencies": {
    "@prisma/client": "6.3.0"
  },
  "devDependencies": {
    "typescript": "5.2.2",
    "prisma": "6.3.0"
  }
}