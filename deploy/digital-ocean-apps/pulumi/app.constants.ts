import config from './src/config';

export const API_NAME = `${config.PROJECT_NAME}-api-${config.APP_ENV}`;
export const SCHEDULER_NAME = `${config.PROJECT_NAME}-scheduler-${config.APP_ENV}`;
export const MIGRATOR_NAME = `${config.PROJECT_NAME}-migrator-${config.APP_ENV}`;
export const WEB_NAME = `${config.PROJECT_NAME}-web-${config.APP_ENV}`;
